# Seal Status

Status: **IN VALIDATION**

Target: Hermes Worker Studio 2.0 — Hermes-native single-runtime archive baseline.

Pinned Hermes snapshot: `9eb832aad74547aaa0c4e6b4c1fab11f7d6a4bea` (`0.20.6`).

This file is intentionally updated only after repository CI and target-machine evidence are known. A green repository CI permits `ARCHIVE CANDIDATE`; `SEALED` additionally requires every target-machine item in `SEAL_CHECKLIST.md`.
